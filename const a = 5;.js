const a = 7;
const b = 6;

let c; // a > b c = tru, a < b c = false


if (a > b) {
    c = true;
    } else {
        c = false;
    }

    c = getData(a,b);
    console.log(c);

    function getData(a,b) {
        return a> b ? true : fals
    }